

# notes

data/auth/GoogleAuthClient -> default values -> constructor


di/AppContainer ~= App + repository implementations, factory

model/* everything has Firebase notation, 
    i'm using DTO so i'll change that to or own model (no framework)


ui/login/LoginScreen -> bring
viewModels/login/LoginViewModel -> bring
