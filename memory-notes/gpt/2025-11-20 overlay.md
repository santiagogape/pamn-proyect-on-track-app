En **Jetpack Compose**, el concepto de “overlay” no es un único composable específico, sino una _técnica_ para dibujar algo **encima** de tu UI.

Dependiendo del tipo de overlay que quieras, se logra con distintos composables:

---

# ✅ 1. **Box** (la forma más común)

La forma estándar de hacer un overlay es:

```kotlin
Box {
    Content()

    Box(
        Modifier
            .matchParentSize()
            .background(Color.Black.copy(alpha = 0.6f))
    ) {
        OverlayContent()
    }
}
```

**Box** dibuja sus hijos en orden ⇒ lo último queda arriba.

---

# ✅ 2. **Dialog** (overlay modal tipo popup)

Si quieres un overlay que capture input:

```kotlin
AlertDialog(
    onDismissRequest = { ... },
    confirmButton = { ... },
    title = { Text("Title") },
    text = { Text("Message") }
)
```

O un dialogo custom:

```kotlin
Dialog(onDismissRequest = { ... }) {
    Box(Modifier.background(Color.White)) {
        Text("Overlay")
    }
}
```

---

# ✅ 3. **Popup** (overlay flotante, no modal)

```kotlin
Popup {
    Text("Esto está SOBRE la UI")
}
```

---

# ✅ 4. **BoxWithConstraints** (overlay dependiente de layout)

Si necesitas saber tamaños para posicionamiento del overlay:

```kotlin
BoxWithConstraints {
    Content()

    Box(
        Modifier
            .offset(x = maxWidth / 2)
            .background(Color.Red)
    ) {
        OverlayContent()
    }
}
```

---

# ✅ 5. **Scaffold → Snackbar / Drawer / BottomSheet**

Scaffold tiene overlays **integrados**:

- Snackbar (overlay temporal abajo)
    
- Modal bottom sheet
    
- Navigation drawer
    
- Top app bar over content
    

```kotlin
Scaffold(
    snackbarHost = { SnackbarHost(hostState) }
) {
    ScreenContent()
}
```

---

# ¿QUÉ TIPO QUIERES?

Dime si buscas:

- 🔹 Overlay semitransparente “loading”
    
- 🔹 Overlay modal tipo popup
    
- 🔹 Overlay de menú contextual
    
- 🔹 Overlay que siga al dedo (drag)
    
- 🔹 Overlay permanente (hud)
    

Y te hago el composable exacto para tu caso.